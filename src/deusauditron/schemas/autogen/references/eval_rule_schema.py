# generated by datamodel-codegen:
#   filename:  references/eval_rule.schema.json

from __future__ import annotations

from enum import Enum

from pydantic import BaseModel, ConfigDict, Field


class Type(Enum):
    """
    Type of evaluation rule
    """

    turn = "turn"
    node = "node"
    flow = "flow"
    intent = "intent"


class EvalRule(BaseModel):
    model_config = ConfigDict(
        extra="forbid",
    )
    name: str = Field(..., description="Name of the evaluation rule")
    instruction: str = Field(
        ...,
        description="Instruction for model to figure out wheter the response is correct or incorrect for the given evaluation rule",
    )
    type: Type = Field(..., description="Type of evaluation rule")
