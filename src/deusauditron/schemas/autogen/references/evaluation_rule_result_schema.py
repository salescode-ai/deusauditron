# generated by datamodel-codegen:
#   filename:  references/evaluation_rule_result.schema.json

from __future__ import annotations

from typing import List, Optional

from pydantic import BaseModel, ConfigDict, Field

from . import eval_rule_schema


class ModelResult(BaseModel):
    model_config = ConfigDict(
        extra="forbid",
    )
    model_name: str = Field(..., description="Name of the model")
    result: str = Field(..., description="Evaluation result for the model")
    reasoning: Optional[str] = None


class EvaluationRuleResult(BaseModel):
    model_config = ConfigDict(
        extra="forbid",
    )
    rule: eval_rule_schema.EvalRule
    model_results: List[ModelResult] = Field(
        ..., description="List of model evaluation results"
    )
