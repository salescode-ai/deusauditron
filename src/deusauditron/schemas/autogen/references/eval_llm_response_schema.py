# generated by datamodel-codegen:
#   filename:  references/eval_llm_response.schema.json

from __future__ import annotations

from enum import Enum

from pydantic import BaseModel, ConfigDict, Field


class Result(Enum):
    """
    Evaluation result indicating pass, fail, or not applicable
    """

    PASS = "PASS"
    FAIL = "FAIL"
    N_A_ = "N.A."
    NO_EVAL = "NO_EVAL"


class EvalLLMResponse(BaseModel):
    """
    Schema for LLM evaluation response containing result and reasoning
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    result: Result = Field(
        ..., description="Evaluation result indicating pass, fail, or not applicable"
    )
    reason: str = Field(
        ...,
        description="A detailed, evidence-based explanation for the evaluation decision",
    )
